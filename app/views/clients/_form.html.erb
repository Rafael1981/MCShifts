<%= form_for(@client) do |f| %>
  <% if @client.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@client.errors.count, "error") %> prohibited this place from being saved:</h2>

      <ul>
      <% @client.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="row">
    <div class='col-sm-3'>
      <div class="form-group">
        <%= f.text_field :name, :placeholder => "Name", class: "form-control"  %>
      </div>
      <div class="form-group">
        <%= f.submit "Save Client", class: "btn btn-danger" %>
      </div>
    </div>
  </div>
<% end %>

