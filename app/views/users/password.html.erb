<h1>Editing User Password</h1>

<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation" class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
      <h4><%= pluralize(@log.errors.count, "error") %> prohibited this log from being saved:</h4>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class='col-sm-3'>
      <div class="form-group">
        <%= f.password_field :password, :placeholder => "New Password", class: "form-control" %>
      </div>
      <div class="form-group">
        <%= f.submit "Save new Password", class: "btn btn-danger" %>
      </div>
    </div>
  </div>
<% end %>


<%= link_to 'Show', @user,  class: "btn btn-link mclink"%> |
<%= link_to 'Back', users_path, class: "btn btn-link mclink"%>
